[mysqld]
# Match Railway production settings to eliminate HEAP-to-ondisk conversion bottleneck
tmp_table_size=256M
max_heap_table_size=256M
innodb_buffer_pool_size=512M
table_open_cache=2000
max_connections=100

# Case-insensitive table names (moved from docker-compose command)
lower-case-table-names=2
