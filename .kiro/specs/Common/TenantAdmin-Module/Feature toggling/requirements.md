Recommendation: Create a small spec for this (requirements + design)
The Sysadm should maintain a list of functions per module that can be switched on or off
The tenant administrator should be able to toggle the availability of these functions for its users

A list of potential features(current but I think many will follow)
FIN:
- Import Banking Accounts: STR Channel Revenue (Calculation)
- Import Invoices Generate Receipt
- Import Invoices Generate Missing Invoices

STR:
- STR Pricing model

Question:
- How difficult is it to implement this and the imopacy on the xisting coide base
-How do we distinct this features with the Module management