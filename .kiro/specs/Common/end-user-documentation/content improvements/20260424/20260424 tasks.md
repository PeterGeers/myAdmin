# Documentation Content Improvement Tasks

Reference: `20260424 findings.md` and `20260424 feedback.md`

---

## Phase 1: Remove Test vs Production (Finding 1 — Decision: Option A)

- [x] 1.1 Delete `docs/docs/getting-started/test-vs-production.md` (NL)
- [x] 1.2 Delete `docs/docs/getting-started/test-vs-production.en.md` (EN)
- [x] 1.3 Remove "Test vs Productie" nav entry from `docs/mkdocs.yml`
- [x] 1.4 Remove "4. Test or Production mode" section from `docs/docs/getting-started/first-login.en.md`
- [x] 1.5 Remove "4. Test of Productiemodus" section from `docs/docs/getting-started/first-login.md`
- [x] 1.6 Check `frontend/src/components/help/helpLinks.ts` — remove any mapping to test-vs-production page (none found)
- [x] 1.7 Verify mkdocs builds without errors (clean build, also fixed broken links in index, faq, overview pages)

## Phase 2: Expand Chart of Accounts (Finding 3 — Decision: "Yes please")

- [x] 2.1 Rewrite Chart of Accounts section in `docs/docs/tenant-admin/tenant-settings.en.md` covering:
  - What it is (initial model is an example, fully customizable)
  - Customization workflow (Export → modify → Import, column structure must stay the same)
  - Ledger account parameters (list each parameter option, how to edit)
  - Critical fields for banking import (`bankaccount = true`, `iban` = bank account number)
  - Accounts in use (protected from deletion, how to restructure)
  - Reference number usage (tracking within ledger account, linking between accounts for debtor/creditor flows)
- [x] 2.2 Translate and apply the same changes to `docs/docs/tenant-admin/tenant-settings.md` (NL)
- [x] 2.3 Verify mkdocs builds without errors

## Phase 3: Clarify Parameter System (Finding 4)

- [x] 3.1 Expand Storage tab section in `docs/docs/tenant-admin/tenant-settings.en.md`:
  - Three provider options explained (Google Drive, S3 Shared, S3 Tenant)
  - Google Drive: folder IDs (root, invoices, templates), how to find them
  - Credential upload / OAuth flow
  - Test Connection button
- [x] 3.2 Add a "How Parameters Work" paragraph to tenant-settings.en.md explaining:
  - Parameters are pre-configured with defaults when modules are activated
  - Tenant admins customize through the structured tabs (Storage, Financial)
  - Branding parameters affect invoice/document appearance
  - Field config parameters control form field visibility
- [x] 3.3 Add Advanced tab documentation to `docs/docs/admin/` section (SysAdmin audience):
  - Raw parameter table, scope inheritance (system → tenant → role → user)
  - System-scope = defaults, tenant-scope = overrides
- [x] 3.4 Translate all changes to NL counterparts
- [x] 3.5 Verify mkdocs builds without errors

## Phase 4: Restructure Onboarding Page (Finding 2 — Decision: "Improve the onboarding page")

- [x] 4.1 Restructure `docs/docs/getting-started/onboarding.en.md` using MkDocs Material content tabs:
  - Group modules into tabbed sections (Banking & Invoices, ZZP, STR & Pricing, Reports & Tax)
  - Keep existing content per module, just reorganized into tabs
- [x] 4.2 Add call-to-action at end of `docs/docs/getting-started/first-login.en.md`: link to onboarding page
- [x] 4.3 Apply same changes to NL counterparts (`.md` files)
- [x] 4.4 Verify mkdocs builds and tabs render correctly

## Phase 5: Consistency Audit (Finding 5)

- [x] 5.1 Audit all pages for duplicate content — ensure each concept has one authoritative page, others cross-reference
- [x] 5.2 Check Storage tab docs vs `invoices/google-drive.md` for overlap — added cross-references (complementary, not duplicate)
- [x] 5.3 Add search keywords to key pages (e.g., "grootboek", "ledger", "rekeningschema" on Chart of Accounts). Also fixed remaining stale "Test mode" references in banking/index, common-workflows
- [x] 5.4 NL/EN sync check — verified all EN changes from phases 1-4 are reflected in NL versions (test passes)
- [x] 5.5 Verify mkdocs builds without errors

## Phase 6: Screenshots (Finding 7 — Decision: "Yes please")

- [x] 6.1 Create folder structure `docs/docs/assets/images/` with subfolders per section
- [x] 6.2 Capture and annotate priority screenshots (provided by user):
  - Login screen (`login.png`)
  - Chart of Accounts table (`nl_rekeningschema.png`)
  - Bank account parameter detail (`nl/uk-ledger-bank-account-parameter.png`)
  - Storage tab overview (`nl_storage_tab.png`)
  - Google Drive configuration (`nl/uk_storage_gdrive.png`)
  - Banking import flow (`nl/uk-bank-import.png`)
  - Banking functions overview (`nl/uk-functions.png`)
- [x] 6.3 Add screenshots to relevant documentation pages using image syntax with width attributes
- [x] 6.4 Verify images render correctly in both NL and EN builds (language-specific images: nl- prefix for NL, uk- prefix for EN)
- [x] 6.5 Verify mkdocs builds and images are included in output
