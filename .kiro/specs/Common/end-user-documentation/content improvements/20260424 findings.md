# Test vs Production — when to use which mode
-- This is an system development function and not a tenant function
-- the function as defined on page /docs/en/getting-started/test-vs-production/ is not available in the app at all. There is a duiplicate with similar text in docs/en/getting-started/first-login/#4-test-or-production-mode


# Tenant administrator
- docs/en/tenant-admin/tenant-settings/#financial-tab



## docs/en/getting-started/first-login/
-- This function should be extended with freelance module To avoid long text in 1 page it would be nice if we could have module with a click through FIN, ZZP, STR

## /docs/en/tenant-admin/tenant-settings/?h=#chart-of-accounts
-- Please improve Chart of accounts
---- The initial model is just an example
---- That the model can be changed by the user to support his requirements
---- Prerequisites the column and column names remain the same
---- Ability to download the current model update  and reload
---- Special focus on the ledger account parameters (list the current parameter  options here and how to edit them
---- That the basis of the ledger account is required for the model to use
---- That the vankaccount = true and iban = Bank account number is key for the import of banking statements

## Check if abvailable otherwise add section abut advanced parameters
-- Please add another step to manage the parameters
---- List the current list of parameters and explain what they do
---- extra focus on Storage parameters (gdrive, s3 shared and s3) and their related parameters for gdrive

## Overall
-- Please check consitency 
---- Some items seem to be documented on more then 1 place Test vs Production 
---- pages per mitem wher various flows can lead to the same item
---- All pages can be found by the search (if possible on multuiple key words)
---- Ensure proper languiage support in NL and UK