When I import an invoice of a new (not used in an account) reference number/folder name. It shows the pdf scan result as expected. But it does not show the proposed record. MNaybe due to the fact there is no eralier version of it. The correct behaviour is that an transaction record wuill be shown (with and without VAT) and the user has to define the opposite accounts and the reference number manually