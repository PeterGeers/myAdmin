# Intentionally (near-)empty function package for the Members Lambda.
#
# The entire module ships in MembersLayer (sam/members/layer), so the function's
# own code bundle carries no application code — the handler
# `sam.members.handler.app.handler` resolves from the layer's `python/` path.
# SAM still needs a non-empty CodeUri directory to package, which this provides.
