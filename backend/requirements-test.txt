pytest==8.4.2
pytest-mock==3.15.1
pytest-xdist==3.5.0
pytest-cov==6.2.1
hypothesis==6.92.1
# ruff: CANONICAL pinned version — keep in exact sync with the `pip install ruff==…`
# lines in .github/workflows/backend-code-quality.yml and full-test-suite.yml. A ruff
# upgrade can expand/enable rule families and turn the lint job red overnight (see
# .kiro/specs/code-quality-maintenance/), so bump it here + in both workflows together,
# deliberately, never as a floating range.
ruff==0.16.5